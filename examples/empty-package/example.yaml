routes:
  - method:
      prefix: /
    cluster:
      name: local
    observe:
      log:
        name: stdout

clusters:
  - name: local
    round_robin:
      - localhost:9001

observe:
  logs:
    - name: stdout
      file: /dev/stdout
